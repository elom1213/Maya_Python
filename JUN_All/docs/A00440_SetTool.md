# A00440_SetTool — Set Tool (사용 안내)

컴포넌트를 원소로 갖는 **마야 오브젝트 세트들끼리 집합론의 기본 연산**을 수행하는 툴이다.
합집합 · 교집합 · 차집합으로 새 세트를 만들고, 씬에서 고른 컴포넌트를 기준으로
세트 하나를 두 조각으로 **분할(split)** 한다.

탭은 셋이다 — **`Edit`**(있는 세트를 다룬다, v01.00 의 기능 전부) ·
**`Create`**(오브젝트마다 세트를 하나씩, 또는 **전부를 담은 세트 하나**를 만든다, v01.01 · v01.05) ·
**`Find`**(오브젝트들이 **어느 세트에 들어 있는지** 찾는다, v01.04).

- 버전: `v01.05` (`app/config/version.py`) — `Create` 에 **Mode**(오브젝트마다 / **전부를 담은 세트 하나**, §7) · `Edit` 에 **`Objects` 리스트 + `Add Objects to Sets`**(§2.1) · v01.04 는 **탭 3개 : `Edit` / `Create` / `Find`** (§7, §9)
- 위치: `JUN_All/tools/A00440_SetTool`
- 형태: 아키텍처 (B) — Maya 내 PySide 툴. 집합 연산 로직은 `maya.cmds` 무의존

---

## 1. 설치 / 실행

### 드래그&드롭 설치
`__dragDrop_A00440.py` 를 Maya 뷰포트로 드래그&드롭 → 현재 셸프에 **`SetTool`** 버튼 설치.

셸프 버튼 아이콘은 툴 폴더 안 `icon/A00440_SetTool.png`(32×32) 을 쓴다 — 겹친 두 원(집합 A·B)에
교집합만 불이 들어온 그림. 파일이 없으면 마야 기본 `pythonFamily.png` 로 폴백한다. 벡터 원본은
같은 폴더의 `.svg` 이고, 고칠 때는 svg 를 고친 뒤 `dev/build_icons.py` 로 다시 래스터화한다.

### 코드로 실행
```python
import tools.A00440_SetTool as A00440_SetTool
A00440_SetTool.run(True)     # True 면 DEV_MODE 에서 리로드
```

---

## 2. 연산

| 버튼 | 식 | 결과 |
|------|-----|------|
| `Union ( ∪ )` | A ∪ B ∪ … | 어느 한 세트에라도 들어 있는 원소 전부 |
| `Intersection ( ∩ )` | A ∩ B ∩ … | 모든 세트에 공통으로 들어 있는 원소 |
| `Difference ( ∖ )` | A ∖ B ∖ … | **첫 세트**에서 나머지 세트의 원소를 뺀 것 |
| `Split ( A ∖ S , A ∩ S )` | A = (A ∖ S) ⊔ (A ∩ S) | A 에서 씬 선택 S 에 해당하는 원소를 빼내 새 세트로 |

- ∪ / ∩ 은 **리스트에 담긴 모든 세트**를 순서대로 접어(fold) 계산한다. 2개 이상 필요.
- ∖ 는 **순서가 의미를 갖는 유일한 연산**이다. 리스트의 **첫 항목이 감수(minuend)**.
  순서는 TSL 의 `Up`/`Down`/`Reverse` 로 바꾼다.
- 결과는 **항상 새 세트**로 만든다(Split 제외). 원본 세트는 건드리지 않는다.

### 결과 세트 이름
`New Set Name` 이 비어 있으면 `union_set` / `intersection_set` / `difference_set` /
`<A>_split` 로 자동 명명한다. 이름이 겹치면 **마야가 뒤에 번호를 붙이므로 실제 이름은 다를 수
있고**, 로그에는 실제로 붙은 이름이 찍힌다.

## 2.1 Sets x Objects — 오브젝트를 세트에 넣기 (v01.05~)

`Edit` 탭의 `Sets` 리스트 **오른쪽에 `Objects` 리스트**가 있다. 세트끼리의 연산(∪ ∩ ∖)은
`Sets` 리스트만 쓰고, 이 리스트는 **세트에 넣을 오브젝트**를 담는다.

| 버튼 | 하는 일 |
|------|---------|
| `Add Objects to Sets  ( obj -> Set )` | `Objects` 의 오브젝트를 `Sets` 의 세트에 **넣는다**(새 세트를 만들지 않는다) |

**짝 규칙**

- `Sets` 에 **세트가 하나**면 → `Objects` 의 **오브젝트 전부**가 그 하나에 들어간다.
- `Sets` 가 **여럿**이면 → **행 순서로 1:1** (`Objects[i]` → `Sets[i]`).
  개수가 다르면 **적은 쪽만큼**만 돌고, 몇 개가 짝지어졌는지 로그에 적는다.

```
Sets            Objects              결과
[ setA ]        [ a , b , c ]        setA <- a, b, c        (세트가 하나 = 전부 그 하나에)

[ setA ]        [ a , b ]            setA <- a
[ setB ]                             setB <- b              (여럿 = 행 순서 1:1)

[ setA ]        [ a , b ]            setA <- a
[ setB ]                             setB <- b
[ setC ]                             setC 는 짝이 없어 건너뜀 (로그에 남는다)
```

- 이미 그 세트에 들어 있는 오브젝트는 **중복되지 않는다**(마야가 무시한다). 대신 몇 개가 이미
  있었는지 로그에 적는다 — 눌렀는데 아무 일도 없는 것처럼 보이지 않도록.
- 세트가 아닌 것이 `Sets` 에 있으면 **아무것도 하지 않고** 사유를 알린다. 씬에 없는 오브젝트는
  건너뛰고 경고한다. 전체가 **undo 한 스텝**.
- 이 자리는 앞으로 `Sets` x `Objects` 연산이 더 붙는 칸이다(그래서 버튼 위에 작은 제목이 있다).

---

---

## 3. Split — 이 연산의 수학적 이름

> "집합 A 에서, 지금 선택한 것들 중 A 에 속한 것만 빼내 새 집합 B 를 만든다"

집합론에 이 동작 **전체**를 가리키는 표준 연산자 이름은 없다. 다만 결과를 이루는 조각들에는
정확한 이름이 있다.

| 조각 | 이름 |
|------|------|
| A ∩ S | **자취 (trace of S on A)** — 부르바키의 용어 |
| A ∖ S | **상대여집합 (relative complement of S in A)** — A 에 대한 S 의 여집합 |
| {A∖S, A∩S} | **S 가 A 에 유도하는 분할 (partition of A induced by S)** |

두 조각은 서로소이고 합치면 A 가 된다 → `A = (A ∖ S) ⊔ (A ∩ S)` (⊔ 는 서로소 합집합).
원소가 두 덩이로 나뉘므로 **이분할(bipartition)** 이라 부르기도 한다.

구현 세계에서는 보통 **partition** 또는 **split** 이라 부른다
(Ruby `Enumerable#partition`, Rust `Iterator::partition` 등이 같은 동작이다).
그래서 이 툴도 버튼 이름을 `Split` 으로 했다.

### 쓰는 법

1. 씬에서 빼낼 컴포넌트를 고른다
2. **`Capture Selection ( S )`** 를 누른다 ← **먼저 누를 것**
3. 리스트에서 A 를 고른다 (한 행만 고르면 그것, 아무것도 안 고르면 첫 행)
4. `Split ( A ∖ S , A ∩ S )`

> ⚠️ **왜 Capture 를 먼저 눌러야 하나**
> 리스트의 행을 클릭하면 공용 TSL 위젯이 그 노드를 씬에서 선택하는데, 대상이 **세트**면
> `cmds.select` 가 세트를 **펼쳐서 멤버 전체를 선택**한다. 그러면 S 가 A 의 멤버 전체가 되어
> A 를 통째로 빼내는 사고가 난다.
> 툴은 이 상황(씬 선택 == A 의 멤버 전체)을 감지해 **실행을 거부하고** 이유를 로그에 적는다.
> Capture 해 둔 게 있으면 그것을 우선 사용한다.

### `Remove the extracted elements from A`

| 상태 | 결과 |
|------|------|
| **켬**(기본) | A 는 A∖S 만 남고, 새 세트가 A∩S 를 가져간다 → **진짜 분할** |
| 끔 | A 는 그대로 두고, 새 세트에 A∩S 를 **복사**만 한다 |

A 의 원소가 전부 빠져 A 가 비어도 **세트 노드 자체는 지우지 않는다**(로그로 알린다).

---

## 4. 컴포넌트 종류가 섞이면

지금은 "모든 세트가 같은 종류의 컴포넌트로 구성된다"고 가정해도 되지만,
**연산 자체는 종류를 가리지 않게** 만들어 두었다. 버텍스와 엣지는 그냥 서로 다른 원소이므로
∪ / ∩ / ∖ 는 그대로 성립한다(버텍스 세트 ∩ 엣지 세트 = 공집합).

- 막지 않고 **경고만** 한다: 한 세트 안에 종류가 섞였을 때, 세트들끼리 종류가 다를 때
- 로그에 구성이 함께 찍힌다 — `setA : 12 elements (vertex x12)`
- 리스트의 `Info` 버튼으로 언제든 각 세트의 원소 수 / 종류를 확인할 수 있다

인식하는 종류: `vtx`(vertex) · `e`(edge) · `f`(face) · `map`(uv) · `vtxFace` ·
`cv` · `ep` · `pt`(lattice) · `u` / `v`, 그리고 컴포넌트가 아닌 것은 `object`.
모르는 토큰은 토큰 그대로 표시하므로 새 컴포넌트 타입이 와도 깨지지 않는다.

---

## 5. 구조

```
A00440_SetTool/
├─ launch.py                 # run() — 창 재생성 방지 + 테마
├─ __dragDrop_A00440.py
└─ app/
   ├─ config/version.py
   ├─ core/
   │  ├─ set_ops.py          # 순수 집합 연산 (Maya 무의존, 그래서 단독 테스트 가능)
   │  ├─ maya_sets.py        # 세트 읽기/쓰기 + 이름 정규화
   │  └─ set_manager.py      # 검증 → 계산 → 씬 반영 (undo 로 묶음)
   └─ ui/
      ├─ main_window.py      # 창 · 탭(Edit / Create / Find) · 메뉴(Help > Set Notation) · Pin · 로그
      ├─ set_tab.py          # Edit 탭 본문 (집합 연산 · Split)
      ├─ create_tab.py       # Create 탭 본문 (오브젝트마다 세트 하나)
      └─ find_tab.py         # Find 탭 본문 (오브젝트가 속한 세트 찾기)
```

---

## 6. 구현에서 중요한 것 — 이름 정규화

집합 연산의 전제는 **같은 원소가 항상 같은 문자열**이어야 한다는 것이다.
마야는 같은 컴포넌트를 여러 형태로 부른다.

```
pCube1.vtx[0:2]        세트가 돌려주는 축약형
pCube1Shape.vtx[1]     셰이프로 선택했을 때
|grp1|pCube1.vtx[1]    풀 패스
```

그래서 **읽어들이는 모든 경로**에서 `cmds.ls(..., flatten=True, long=True)` 로 한 형태로
통일한다. mayapy 로 확인한 사실들:

- 축약 범위(`vtx[0:2]`)가 `flatten` 으로 펼쳐진다
- **셰이프로 선택해도 트랜스폼 롱네임으로 정규화된다** → 세트 멤버와 씬 선택을 바로 비교해도 된다
- 동명 노드가 있으면 `cmds.sets(q=True)` 가 **구분되는 부분 경로**를 준다(`grp1|pCube1.vtx[0:2]`)
  → 모호하게 뭉개지지 않는다. 반대로 **모호한 짧은 이름을 `cmds.sets()` 에 넘기면 에러**가 나므로
  항상 정규화된 이름으로 넘긴다
- 빈 세트는 `None` 을 돌려준다 → 반드시 `or []` 로 받는다
- `cmds.sets(members, remove=...)` 로 들어 있지 않은 것을 빼도 에러가 아니다(무시)

세트 이름은 TSL 이 **UUID 로 보관**하므로 담아 둔 뒤 리네임해도 올바른 노드를 찾는다.

### 그 밖에
- 순서 보존: 판정에만 `set` 을 쓰고 결과는 `dict` 로 만들어 **원소 순서를 유지**한다
- 세트 안의 세트는 펼치지 않고 **원소 하나**로 센다(경고로 알린다)
- 씬을 바꾸는 연산은 전부 `undo_chunk()` — **Ctrl+Z 한 번**으로 되돌아간다

---

## 7. Create 탭 — 세트 만들기 (v01.01 · Mode v01.05~)

**Mode** 로 방식을 고른다.

| Mode | 만드는 것 | 이름 |
|------|-----------|------|
| `One set per object` (기본, v01.01~) | 오브젝트마다 하나 — **세트 개수 = 오브젝트 개수** | `<오브젝트 이름>_Set` |
| `One set for all` (v01.05~) | 리스트의 **전부를 담은 세트 하나** | `Set Name` 칸에 적은 것, 비우면 `objects_Set` |

`One set for all` 은 `Set Name` 칸이 켜지고 버튼 글자가 `Create One Set` 으로 바뀐다.
이름은 per-object 와 **같은 규칙으로 다듬는다**(경로·네임스페이스를 떼고 이상한 글자를 치환 —
아래 7.1). 겹치면 마야가 번호를 붙이므로 실제 이름을 로그에 적는다(7.2).

아래는 기본 모드(`One set per object`) 설명이다.

```
Objects                      -> 만들어지는 세트
[ ctl_l_arm  ]                  ctl_l_arm_Set   { ctl_l_arm }
[ ctl_r_arm  ]                  ctl_r_arm_Set   { ctl_r_arm }
[ ctl_spine  ]                  ctl_spine_Set   { ctl_spine }
```

- `Create Sets` : 리스트 순서대로 만든다. 전체가 **undo 한 스텝**.
- `Add the new sets to the Edit tab list` (기본 ON) : 만든 세트를 `Edit` 탭 리스트에 담고
  탭을 그쪽으로 넘긴다 — 방금 만든 세트가 곧 다음에 합칠 것들이기 때문이다.
- `Select the new sets in the scene` : 만든 **세트 노드**를 선택한다.
  - ★ 세트를 그냥 `select` 하면 마야가 **멤버를 펼쳐** 선택한다. 세트 자체를 고르려면
    `noExpand` 가 필요하다(`maya_sets.select_sets`). 이 툴이 §6 에서 말하는 함정과 같은 것이다.

### 7.1 이름은 우리가 다듬는다 ★

**경로와 네임스페이스를 뗀다** — `|grp|rig:pCube1` → `pCube1_Set`.
네임스페이스를 남기면 마야가 세트를 **그 네임스페이스 안에** 만든다(레퍼런스면 곤란해진다).

마야에 이름을 그대로 넘기면 **조용히 고친다**(실측):

| 넘긴 이름 | 마야가 만든 이름 | |
|---|---|---|
| `pCube1.vtx[0]_Set` | `pCube1_vtx_0__Set` | 못 쓰는 문자를 `_` 로 |
| `1_Set` | `_Set` | **앞 숫자를 버린다** |
| `rig:pCube1_Set` | `rig:pCube1_Set` | 그 네임스페이스 안에 만든다 |

앞 숫자를 버리는 것이 특히 나쁘다 — 다만 **씬에서 온 이름은 애초에 숫자로 시작할 수 없다**
(마야가 노드·그룹·네임스페이스 이름에서 똑같이 버린다: `01_arm` → `_arm`, `9grp` → `grp`,
네임스페이스 `1ns` → `ns`). 그래도 손으로 넣은 이름이 들어올 수 있으므로 숫자로 시작하면
앞에 `_` 를 둔다.

### 7.2 이름이 겹치면 짚는다

같은 이름이 이미 있으면 마야가 뒤에 번호를 붙인다(`pCube1_Set` → `pCube1_Set1`).
**세트가 아닌 노드**가 그 이름을 쓰고 있어도 마찬가지다. 밀린 것은 **하나하나 경고로 적는다** —
이름을 보고 찾을 사람에게는 조용히 밀리는 것이 가장 나쁘다.

짧은 이름이 같은 두 오브젝트(`|grpA|ctl` · `|grpB|ctl`)를 함께 넣으면 `ctl_Set` 과
`ctl_Set1` 이 되고, 둘 다 로그에 남는다.

씬에 없는 항목은 **건너뛰고 몇 개였는지 적는다.** 리스트가 비었으면 아무것도 만들지 않는다.

---

## 9. Find 탭 — 이 오브젝트들은 어느 세트에 있나 (v01.04)

`Create` 가 세트를 만들고 `Edit` 가 세트를 다룬다면, `Find` 는 **거꾸로 묻는다** —
"내가 고른 이 오브젝트들이 **어느 세트에 들어 있지?**"

```
Objects                        Find Sets          Sets
[ ctl_l_arm ]        ───────────────────────▶    [ arm_Set    ]   ← 클릭하면 씬에서
[ ctl_l_hand ]                                    [ l_side_Set ]     **그 세트 노드**가 선택
[ pCube1.vtx[0] ]                                 [ compSet1   ]     (Shift / Ctrl 로 여러 개)
```

### 쓰는 법

1. 위 `Objects` 리스트에 오브젝트를 담는다 (`Select Objects` / `Add`).
   **컴포넌트(`pCube1.vtx[0]`)도 그대로 넣을 수 있다.**
2. `Find Sets` — 그것들이 멤버로 들어 있는 세트가 아래 `Sets` 리스트에 올라온다.
   **씬은 아무것도 바뀌지 않는다.**
3. 아래 리스트의 행을 클릭한다 → **그 세트가 씬에서 선택된다.**
   **Shift / Ctrl 로 여러 행**을 고르면 그만큼 여러 세트가 선택된다.

로그에는 세트마다 **몇 개가 걸렸는지**가 함께 찍힌다 — `arm_Set : 2 / 3 object(s)`.
결과 리스트는 찾을 때마다 **교체**된다(지난 결과가 섞이면 어느 것이 이번 결과인지 알 수 없다).

| 버튼 | 동작 |
|------|------|
| `Info` | 리스트에 올라온 세트마다 원소 수 / 종류를 로그에 찍는다 |
| `To Edit` | 고른 세트(아무것도 안 골랐으면 전부)를 `Edit` 탭 리스트로 보낸다 → 바로 ∪ / ∩ / ∖ |

### 옵션

| 옵션 | 기본 | 뜻 |
|------|:----:|-----|
| `Look at the shape as well` | ON | **컴포넌트 세트와 셰이딩 그룹은 셰이프에 붙는다.** 끄면 트랜스폼이 직접 들어간 세트만 나온다 |
| `Include shading groups` | OFF | 셰이딩 그룹(`shadingEngine`)도 세트다. 켜면 `initialShadingGroup` 까지 보인다 |
| `Include parent sets` | OFF | 세트를 **멤버로 갖는** 세트까지 위로 따라간다 (오브젝트 ∈ B, B ∈ C → C 도) |
| `Include Maya's default sets` | OFF | `defaultLightSet` · `defaultObjectSet` … |
| `Select the found sets in the scene` | OFF | 찾자마자 결과 세트를 전부 선택 |

> `initialShadingGroup` 은 **렌더링 세트이면서 마야 기본 노드**다. 두 스위치가 서로를 막지
> 않도록 **렌더링 세트는 `Include shading groups` 하나로만** 판정한다.

### 9.1 ★ 행을 클릭하면 "세트" 가 선택된다 — noExpand

이 툴이 §6 과 §7 에서 계속 짚는 그 함정이다. `cmds.select` 는 **세트를 펼쳐 멤버를 선택**한다.
그대로 두면 `Sets` 리스트에서 행을 눌렀을 때 세트가 아니라 **멤버 전체**가 잡힌다.

그래서 공용 TSL 위젯에 **`select_no_expand`** 옵션을 새로 두고(→ [`Framework_MOD_tsl_qt.md`](Framework_MOD_tsl_qt.md) §2.1)
`Find` 탭의 결과 리스트에서 켰다. 행 클릭이 `noExpand=True` 로 나가 **세트 노드 자체**가 선택된다.

**`Edit` 탭은 예전 동작 그대로 둔다** — 그쪽 `Split` 은 "씬 선택 == A 의 멤버 전체" 를
사고 감지(§3)에 쓰고 있기 때문이다.

### 9.2 `listSets` 로 확인한 것들 (mayapy 2024 실측)

| 물어본 것 | 돌아온 것 |
|---|---|
| `listSets(o=<트랜스폼>)` | **트랜스폼이 들어간 세트만** — 컴포넌트 세트는 안 나온다 |
| `listSets(o=<셰이프>)` | 컴포넌트 세트 · 셰이딩 그룹 |
| `listSets(o="pCube1.vtx[0]")` | **그 컴포넌트를 담은 세트만** (같은 메시의 다른 컴포넌트 세트는 제외) |
| 면과 버텍스를 함께 담은 세트 | **두 번 나온다** → 중복 제거가 필요하다 |
| `listSets(o="pCube1.tx")` | `None` |
| 없는 이름 | **에러(TypeError)** → 감싸야 한다 |

- **`extendToShape` 는 쓰지 않는다** — 첫 셰이프 하나만 본다. `listRelatives(shapes=True,
  noIntermediate=True)` 로 **셰이프를 전부** 물어본다.
- ★ **`listRelatives` 에 세트를 넘기면 에러가 아니라 세트를 펼쳐 멤버의 셰이프를 준다**
  (`listRelatives(objSet1, shapes=True)` → `['|pCube1|pCube1Shape']`). `select` 가 세트를
  펼치는 것과 같은 함정의 다른 얼굴이라, **DAG 노드인지 먼저 확인**하고 묻는다.
- 셰이딩 그룹은 `nodeType` 이 `shadingEngine` 이다(`objectSet` 의 파생) — `ls(type="objectSet")`
  에는 섞여 나오므로 `nodeType` 비교로 갈라낸다.

---

## 8. 검증

`mayapy` (Maya 2024) 헤드리스 **71 + 34 + 41항목 통과**.

**Find 탭 41항목** — `listSets` 의 트랜스폼/셰이프/컴포넌트 차이 · 중복 제거 ·
없는 이름/어트리뷰트에도 안 죽는지 · 세트를 `listRelatives` 로 물었을 때 멤버가 새어 들지
않는지 · 기본 세트/렌더링 세트 판정 · 부모 세트 추적과 **순환 세트에서 멈추는지** ·
옵션 4개가 각각 결과를 바꾸는지 · 그룹 안 오브젝트의 풀 패스 · 빈 리스트/없는 노드/
아무 세트에도 없는 오브젝트 · **씬이 바뀌지 않는지** · 탭 3개 · 재검색이 지난 결과를
교체하는지 · **행 클릭이 멤버가 아니라 세트를 선택하는지** · 두 행이면 두 세트 ·
`Edit` 탭은 예전대로 멤버를 펼치는지 · `To Edit` · `Select the found sets`.

**Create 탭 34항목** — 탭 구성(기존 UI 가 `Edit` 로 그대로) · 오브젝트 수만큼 세트 · 멤버가
자기 자신 하나 · undo 한 스텝 · 경로/네임스페이스 제거 · 이름 규칙 6가지 · 이름 충돌 경고 ·
짧은 이름이 같은 두 오브젝트 · 빈 리스트/없는 노드 가드 · `Edit` 로 보내기 · 보낸 세트로 Union ·
**세트 선택이 멤버로 펼쳐지지 않는지**.

- 순수 집합 연산 25항목 — 순서 보존, 중복 제거, 3-way, ∖ 의 비가환성, split 경계
- 마야 통합 — 정규화, ∪/∩/∖ 결과, 이름 충돌, 세트 아닌 입력 거부
- **셰이프로 선택 + 트랜스폼으로 저장된 세트** 교차 판정
- Split — 원본 축소 / 복사 모드 / 걸리는 원소 없음 / 빈 선택 / undo 한 스텝
- 동명 노드가 있는 씬에서 원소가 뻥튀기되지 않는지
- 종류 혼합 · 중첩 세트 경고, 오브젝트(비컴포넌트) 세트
- UI 배선 — 위젯 생성, S 캡처, **행 클릭이 멤버를 선택하는 상황에서 Split 이 거부되는지**

---

## 로그창 (v01.02)

로그창은 **공용 위젯 `JUN_mod_log_qt_v01`** 이다. 오른쪽 위에 작은 버튼 셋이 붙어 있다.

| 버튼 | 동작 |
|------|------|
| `Expand` | 로그를 **별도 창으로 옮겨** 크게 본다. 확장 중에 들어온 로그도 같은 곳에 쌓이고, 창을 닫으면 제자리로 돌아온다 |
| `Clear` | 로그를 비운다 |
| `Copy` | 로그 **전문**을 클립보드로 |

자세한 것은 [`Framework_MOD_log_qt.md`](Framework_MOD_log_qt.md).
